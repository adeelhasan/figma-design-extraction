# Figma Personal Access Token
# Used for direct Figma API calls (file structure discovery, etc.)
# The MCP uses its own authentication, but this is needed for REST API calls
FIGMA_ACCESS_TOKEN=<access_token_here>
